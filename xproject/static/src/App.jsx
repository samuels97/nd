var contentNode = document.getElementById('contents');
var component = <h2>Hello World!</h2>; // A simple JSX component
ReactDOM.render(component, contentNode); // Render the component inside the content Node
